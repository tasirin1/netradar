name: Repo Guard

on:
  push:
  pull_request:

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Cek konsistensi repo
        run: |
          python3 scripts/check_repo.py
          python3 scripts/check_repo.py --readme
